// This file contains geometry utility functions (reserved for reuse in future complex problems).
// For this problem, no advanced geometry utilities are needed.
// Example utility included:

/**
 * Converts degrees to radians
 * @param deg 
 */
export function degToRad(deg: number): number {
  return deg * (Math.PI / 180);
}